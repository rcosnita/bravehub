"use strict";

/**
 * Provides a message bus implementation which is used to communicate between various components
 * from the canvas: nodes, scenegraph, gestures, editors.
 */
export default class MessageBus extends EventEmitter { }  // eslint-disable-line no-undef
